const AllLink = document.getElementById("AllLink");

AllLink.addEventListener("click",fetchTasks);

const AllPriority = document.getElementById("AllPriority");

AllPriority.addEventListener("click",fetchTasks);
